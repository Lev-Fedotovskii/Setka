Experimental channel foundation for 0.4. This is not the completed 0.4 feature release.

- PWA: https://lev-fedotovskii.github.io/Setka/unstable/
- Android: **Setka Unstable**, separately installed as `io.setka.app.unstable`.
- Stable remains v0.3.2 at https://lev-fedotovskii.github.io/Setka/ and remains the default download.
- Separate application storage, notification ledger, cache and install identity. No automatic copying of personal data. Optional transfer uses explicit backup/restore in Unstable.
- Stable MIPT source updates continue every six hours while its application stays pinned.

Published only after the API 35/36 emulator release checks pass, including coexistence and Stable-data retention. Physical-device checks are not claimed. The owner decides when 0.4 is ready for Stable; experiments never promote automatically.
